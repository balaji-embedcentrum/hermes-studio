/**
 * Sylang Studio's workspace home — the view shown in the editor area when no
 * file is selected. The shared files screen renders `useBrand().WorkspaceHome`.
 *
 * SCAFFOLD STUB: during Phase 1/2, move Sylang's existing WorkspaceHome content
 * here (the Quick-Action cards that open Coverage / Traceability / FMEA inline
 * views — currently embedded in hermes-studio-sylang `routes/files.tsx`).
 */
export function SylangWorkspaceHome() {
  return (
    <div className="flex h-full items-center justify-center text-ink/60">
      <p>Sylang Studio — open a model file, or pick a Quick Action.</p>
    </div>
  )
}
